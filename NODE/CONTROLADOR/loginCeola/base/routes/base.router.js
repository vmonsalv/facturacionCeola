'use strict'

var express = require('express');

var menuService = require('../services/menu.service');

var router = express.Router();

//router.post('/getApps', menuService.sendApp);

module.exports = router;
